import React from "react";
import ProfileWrapper from "../../../Shared/Profile/ProfileWrapper";

const index = () => {
  return (
    <React.Fragment>
      <ProfileWrapper />
    </React.Fragment>
  );
};

export default index;
