import React from "react";
import PackageWrapper from "../../../Shared/Packagelimit/PackageWrapper";

const index = () => {
  return (
    <React.Fragment>
      <PackageWrapper />
    </React.Fragment>
  );
};

export default index;
